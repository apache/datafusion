# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at

#   http://www.apache.org/licenses/LICENSE-2.0

# Unless required by applicable law or agreed to in writing,
# software distributed under the License is distributed on an
# "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
# KIND, either express or implied.  See the License for the
# specific language governing permissions and limitations
# under the License.

statement ok
set datafusion.sql_parser.dialect = databricks;

statement ok
CREATE TABLE t AS SELECT 1 as x;

query II
SELECT
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> a+1),
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> a+x)
from t;
----
2 2

query II
SELECT
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> b+1),
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> b+x)
from t;
----
3 3

query II
SELECT
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> c+1),
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> c+x)
from t;
----
4 4

query II
SELECT
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> a+b+1),
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> a+b+x)
from t;
----
4 4

query II
SELECT
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> a+c+1),
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> a+c+x)
from t;
----
5 5

query II
SELECT
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> b+c+1),
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> b+c+x)
from t;
----
6 6

query II
SELECT
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> a+b+c+1),
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> a+b+c+x)
from t;
----
7 7

query II
SELECT
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> 1),
    multi_arg_higher_order_udf(1, 2, 3, (a, b, c) -> x)
from t;
----
1 1

statement ok
set datafusion.sql_parser.dialect = generic;

statement ok
DROP TABLE t;
