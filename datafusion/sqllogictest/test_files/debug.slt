statement ok
CREATE TABLE temp
AS VALUES
    ([1,2,3],1,[9,10]),
    ([1,2,3],2,[11,12]),
    ([4,5,6],2,[13,14])

query TT
explain select unnest(column1) c1 from temp group by unnest(column1);
----

query TT
explain select unnest(column1) c1 from temp group by unnest(column1);
----

query TT
explain select unnest(column1) c1 from temp group by c1;
----
1 1
1 2
1 3
