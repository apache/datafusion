statement ok
create table t (a int, b string, c int) as values (1, 'a', 3), (1, 'a', 3), (1, 'a', 3), (1, 'a', 3), (2, 'a', 1), (2, 'a', 1), (2, 'a', 1), (3, null, 2);

query ITI
select a, b, count(*) from t group by a, b order by count(*) desc;
----
1 a 4
2 a 3
3 NULL 1
