# DENSE_RANK top-2, ~1K partitions.

subgroup window_sorted

template sql_benchmarks/h2o/window_sorted.benchmark.template
QPAD=12
FN=DENSE_RANK
PK_MOD=1000
OB_EXPR=v2
OB_BOUND=count(DISTINCT ob) > 1000
NAME=window_sorted_q12_dense_rank_1k_partitions